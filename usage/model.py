class Model(object):
	def __init__(self): # maybe make data a completely empty list that controller appends all necessary data types to upon creation 
		self.data=[
					['CPU:', None],
				    ['RAM:', None]
				    ]